get:
  - in: query
    name: sellerSkus
    required: false
    schema:
      type: string
    example: "SELLERSKU1,SELLERSKU2"
  - in: query
    name: skus
    required: false
    schema:
      type: string
    example: "SKU1,SKU2"