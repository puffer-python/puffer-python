get:
  - in: query
    name: page
    schema:
      type: integer
      default: 0
    description: Numeric ID of the category
  - in: query
    name: pageSize
    schema:
      type: integer
      default: 10
    description: Numeric ID of the category
  - in: query
    name: query
    schema:
      type: string
    description: name or code
  - in: query
    name: orderBy
    schema:
      type: string
    description: Numeric ID of the category