get:
  - in: query
    name: ids
    schema:
      type: string
    description: ids có dạng 1,2,3
  - in: query
    name: skus
    schema:
      type: string
    description: skus có dạng sku1,sku2,sku3
  - in: query
    name: keyword
    schema:
      type: string
  - in: query
    name: category
    schema:
      type: string
  - in: query
    name: masterCategory
    schema:
      type: string
  - in: query
    name: brand
    schema:
      type: integer
  - in: query
    name: attributeSet
    schema:
      type: integer
  - in: query
    name: sellingStatus
    schema:
      type: integer
    description: id map với id của bảng selling_status
  - in: query
    name: editingStatus
    schema:
      type: integer
    description: id map với id của bảng editing_status
  - in: query
    name: terminal
    schema:
      type: string
    description: terminal code
  - in: query
    name: terminalGroup
    schema:
      type: string
    description: terminal group code
  - in: query
    name: page
    schema:
      type: integer
  - in: query
    name: pageSize
    schema:
      type: integer
  - in: query
    name: export
    schema:
      type: integer
    default: 0
