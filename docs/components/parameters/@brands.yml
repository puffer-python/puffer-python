get:
  - in: query
    name: query
    schema:
      type: string
      default: null
    description: filter like name
  - in: query
    name: codes
    schema:
      type: string
      default: null
    description: filter theo code. allow multiple codes filter
  - in: query
    name: isActive
    schema:
      type: boolean
      default: 1
    example: 1
  - in: query
    name: approvedStatus
    schema:
      type: boolean
      default: 0
    example: 0
  - in: query
    name: pageSize
    schema:
      type: integer
      default: 10
    example: 10
  - in: query
    name: page
    schema:
      type: integer
      default: 1
    example: 1
